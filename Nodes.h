//it is clear that memory leaks can be overcome
//but it is not clear if small programs can hold big datas.
//so, lets keep the kernel datas and classes safe !

class NoOvercomeMixedServices
{
private:
  double thresholdbase...
  double extremeThresholdOfLayerSizeInMemory=....

public:
  //our system may check some.rhings with ai
  double traceGetCuree'tTreshold...
}
